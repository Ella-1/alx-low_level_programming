#include <stdio.h>
/**
 * main - prints alphabet in lowercase followed by a new line.
 *
 * Return: always 0.
 */
int main(void)
{
	int i;
	void print_alphabet();
	for (i = 0; i <= 26; i++)
{
	putchar('a' + c);
}
	putchar('\n');
	return (0);
}
